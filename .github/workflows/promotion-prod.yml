name: Release Promotion Guard (prod)

permissions:
  contents: read

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
    branches:
      - release/prod

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  promotion_prod:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    steps:
      - uses: ./.github/actions/release-promotion-guard.yaml
